version: "3.9"

services:
  bidder:
    build: .
    ports:
      - "8002:8002"
    environment:
      - ENV=development
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8002 --reload
